{
  "name": "s2n",
  "version": "1.5.27",
  "description": "C99 implementation of the TLS/SSL protocols. wire-vcpkg-registry overlay: libcrypto comes from boringssl-custom (drop-in OpenSSL ABI shim) instead of upstream openssl, so the project's TLS stack stays single-source.",
  "homepage": "https://github.com/aws/s2n-tls",
  "license": "Apache-2.0",
  "supports": "!uwp & !windows",
  "dependencies": [
    "boringssl-custom",
    {
      "name": "vcpkg-cmake",
      "host": true
    },
    {
      "name": "vcpkg-cmake-config",
      "host": true
    }
  ],
  "features": {
    "tests": {
      "description": "Build and run the tests"
    }
  }
}
