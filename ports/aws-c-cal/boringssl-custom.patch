diff --git a/CMakeLists.txt b/CMakeLists.txt
index 29ff105..8a75570 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -122,10 +122,9 @@ endif()
 if (NOT BYO_CRYPTO)
     if ((NOT WIN32 AND NOT APPLE) OR AWS_USE_LIBCRYPTO_TO_SUPPORT_ED25519_EVERYWHERE)
         if (USE_OPENSSL AND NOT ANDROID)
-            find_package(OpenSSL REQUIRED)
-            find_package(Threads REQUIRED)
-            list(APPEND PLATFORM_LIBS OpenSSL::Crypto Threads::Threads)
-            message(STATUS "Using libcrypto from system: ${OPENSSL_CRYPTO_LIBRARY}")
+            find_package(boringssl-custom REQUIRED)
+            list(APPEND PLATFORM_LIBS boringssl::crypto)
+            message(STATUS "Using libcrypto from boringssl-custom")
         elseif(NOT USE_OPENSSL AND IN_SOURCE_BUILD)
             if (TARGET crypto)
                 message(STATUS "Using libcrypto from AWS-LC")
diff --git a/cmake/aws-c-cal-config.cmake b/cmake/aws-c-cal-config.cmake
index 7239f8e..6aac15f 100644
--- a/cmake/aws-c-cal-config.cmake
+++ b/cmake/aws-c-cal-config.cmake
@@ -4,10 +4,8 @@ find_dependency(aws-c-common)
 
 if (NOT @BYO_CRYPTO@ AND (@AWS_USE_LIBCRYPTO_TO_SUPPORT_ED25519_EVERYWHERE@ OR (NOT WIN32 AND NOT APPLE)))
     if (@USE_OPENSSL@ AND NOT ANDROID)
-        # aws-c-cal has been built with a dependency on OpenSSL::Crypto,
-        # therefore consumers of this library have a dependency on OpenSSL and must have it found
-        find_dependency(OpenSSL REQUIRED)
-        find_dependency(Threads REQUIRED)
+        # aws-c-cal has been built against boringssl-custom (drop-in libcrypto)
+        find_dependency(boringssl-custom REQUIRED)
     else()
         list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}/modules")
         find_dependency(crypto)
